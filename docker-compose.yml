name: DrBot
services:
  drbot:
    image: ghcr.io/incoverse/drbot:latest
    container_name: DrBot
    restart: on-failure
    stop_signal: SIGINT
    volumes:
      - data:/drbotdata:rw
    env_file: .env
volumes:
  data: