declare global {
  interface WaiterConfig {
    /** Web-specific configuration options */
    web?: {
      /** The port on which the web server will run */
      port?: number;
    };
  }
}


export { };

